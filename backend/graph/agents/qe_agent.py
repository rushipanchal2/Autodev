"""QE Agent (LLM #3) — Phase 2.

reads:  user_stories, code_changes
writes: test_cases (list of {id, title, steps, expected_result})
        optionally merges generated pytest files into code_changes
"""

# TODO Phase 2: implement using same ChatOpenAI + with_structured_output pattern
